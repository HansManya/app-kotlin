fun main(args:Array<String>) {
    var greeting = "Hello World"
    println(greeting.uppercase())
    println(greeting.lowercase())
    println(greeting.length)
    println(greeting.reversed())

    var pyNum = 16.0
    println(Math.sqrt(pyNum))
    println(Math.pow(pyNum, 2.0))
}